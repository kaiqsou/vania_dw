@model IEnumerable<SistemaAcademico.Models.Curso>

@{
    ViewData["Title"] = "Cursos";
}

<h1>Lista de Cursos</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Novo Curso</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) 
{
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.CursoId">Editar</a>
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.CursoId">Detalhes</a>
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.CursoId">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
